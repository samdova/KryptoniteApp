// import File from '../models/fileModel';

// const getAllImages = async () => {
//   return await File.find().populate('user', 'email');
// };

// const getImage = async (id) => {
//   return await File.findById(id).populate('user', 'email');
// };

// module.exports = { getAllImages, getImage };